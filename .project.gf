# gf2 (gf) project configuration — read from the working directory.
# We use Sublime Text as the editor, so disable the gvim-sync that gf2
# otherwise launches at startup (spawns `vim --servername`).
[vim]
enabled=0
